{
  "name": "medchain-ehr",
  "version": "1.0.0",
  "description": "MedChain — Blockchain-Based Electronic Health Record System",
  "private": true,
  "scripts": {
    "install:all":    "cd backend && npm install && cd ../frontend && npm install && cd ../blockchain && npm install",
    "start:backend":  "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "start:chain":    "cd blockchain && npx hardhat node",
    "deploy:chain":   "cd blockchain && npx hardhat run scripts/deploy.js --network localhost"
  },
  "author": "MedChain Team",
  "license": "MIT"
}
